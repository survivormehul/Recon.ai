services:
  postgres:
    image: postgres:16-alpine
    container_name: recon_ai_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: recon_ai
    ports:
      - "5432:5432"
    volumes:
      - recon_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d recon_ai"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  recon_pgdata:
